{
  "name": "smart-farmer-procurement-platform",
  "version": "1.0.0",
  "description": "SIH26032 Smart Agricultural Procurement Platform - Ministry of Consumer Affairs, Food & Public Distribution",
  "private": true,
  "main": "node_modules/expo/AppEntry.js",
  "workspaces": [
    "shared",
    "backend",
    "frontend-farmer",
    "frontend-operator",
    "frontend-admin"
  ],
  "scripts": {
    "start": "npx concurrently -k -n \"BACKEND,FARMER,OPERATOR,ADMIN\" -c \"blue,green,magenta,cyan\" \"npm --prefix backend run dev\" \"npm --prefix frontend-farmer run web\" \"npm --prefix frontend-operator run dev\" \"npm --prefix frontend-admin run dev\"",
    "dev": "npx concurrently -k -n \"BACKEND,FARMER,OPERATOR,ADMIN\" -c \"blue,green,magenta,cyan\" \"npm --prefix backend run dev\" \"npm --prefix frontend-farmer run web\" \"npm --prefix frontend-operator run dev\" \"npm --prefix frontend-admin run dev\"",
    "dev:all": "npx concurrently -k -n \"BACKEND,FARMER,OPERATOR,ADMIN\" -c \"blue,green,magenta,cyan\" \"npm --prefix backend run dev\" \"npm --prefix frontend-farmer run web\" \"npm --prefix frontend-operator run dev\" \"npm --prefix frontend-admin run dev\"",
    "dev:expo": "npm --prefix frontend-farmer run start",
    "dev:backend": "npm --prefix backend run dev",
    "dev:farmer": "npm --prefix frontend-farmer run web",
    "dev:farmer:native": "npm --prefix frontend-farmer run start",
    "dev:operator": "npm --prefix frontend-operator run dev",
    "dev:admin": "npm --prefix frontend-admin run dev",
    "seed": "npm --prefix backend run seed",
    "build": "npm --prefix frontend-farmer run build",
    "build:all": "npm --prefix shared run build && npm --prefix backend run build && npm --prefix frontend-farmer run build && npm --prefix frontend-operator run build && npm --prefix frontend-admin run build",
    "build:backend": "npm --prefix backend run build",
    "start:backend": "npm --prefix backend run start"
  },
  "keywords": [
    "agriculture",
    "procurement",
    "smart-queue",
    "sih26032",
    "mandi",
    "msp"
  ],
  "author": "SIH Team",
  "license": "MIT",
  "dependencies": {
    "vite": "^5.2.11",
    "@vitejs/plugin-react": "^4.3.0"
  },
  "devDependencies": {
    "@types/react": "~19.2.4",
    "concurrently": "^8.2.2",
    "typescript": "~6.0.3"
  }
}
